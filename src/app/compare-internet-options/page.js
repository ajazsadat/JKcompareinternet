import IndependentSupportHelpContent from '@/components/IndependentSupportHelpContent';

export const metadata = {
  title: 'Compare Internet Options | JKcompareinternet',
  description:
    "Call now and we'll walk you through your options. Speak with a live agent or send us a message. JKcompareinternet is an independent comparison resource.",
};

export default function CompareInternetOptionsPage() {
  return <IndependentSupportHelpContent />;
}
